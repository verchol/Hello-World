'use strict';

describe('Filter: success', function () {

  // load the filter's module
  // beforeEach(module('codeFreshDemoApp'));

  // // initialize a new instance of the filter before each test
  // var success;
  // beforeEach(inject(function ($filter) {
  //   success = $filter('success');
  // }));

  // it('should return the input prefixed with "success filter:"', function () {
  //   var text = 'angularjs';
  //   expect(success(text)).toBe('success filter: ' + text);
  // });

});
